# Data — too large for git, regenerate via scripts/prepare_corpus.py
data/raw/
data/processed/

# Model artifacts (regenerate via export_bundle.py / run_post_pretrain.py)
checkpoints/
bundles/

# Internal agent/task tracking — never commit
tasks/
prompts/
ClaudeAgent.md
AGENTS.md
AGENT.md
CLAUDE.md

# Learning documentation — private (whole docs tree stays local)
docs/

# Markdown — only README is published
*.md
!README.md

# Plans and task files
*.plan.md
todo.md
lessons.md
CHANGELOG.md

# Secrets
.env
.env.*
*.pem
*.key

# Python / OS / editor
.venv/
venv/
.pytest_cache/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
.idea/
.vscode/

# AL-1 dev working folders (kept local; mod/ is the published Model A snapshot)
model-a-scratch/
model-b-opensource/
!mod/CHECKLIST.md
